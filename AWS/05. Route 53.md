CNAME: trỏ được về aws và non-aws resource. TUy nhiên chỉ work với non-root domain

Alias: chỉ trỏ được về aws resource (VD ELB, CloudFront distribution, Elastic Beanstalk, API GW, VPC endpoint, S3 website bucket). Work với cả root và non-root domain. Ưu điểm là không mất phí

Root domain là tên miền gốc, ví dụ: “example.com” (kết hợp giữa domain name và TLD).

Non-root domain là mọi biến thể hoặc phần mở rộng từ root domain, ví dụ:

- Subdomain: “blog.example.com”, “shop.example.com” – đây là các non-root domain vì chúng thêm tiền tố phía trước tên miền gốc.

- Địa chỉ có www: “www.example.com” cũng có thể gọi là non-root domain vì “www” là một subdomain mặc định, không phải tên miền gốc thực sự.


Root domain luôn là địa chỉ cơ bản nhất cho một website, ví dụ “tuna-devops.site”.

Non-root domain bao gồm bất cứ thứ gì xuất hiện trước tên miền gốc, thường là các subdomain.

---

Lệnh làm việc với domain

nslookup -type=NS geneco.io 8.8.8.8
NS của domain có trỏ về Route53 không?
nslookup -type=NS geneco.io 8.8.8.8


# Thử query thẳng vào 8.8.8.8 để bypass local cache
nslookup production-genesis-iam-be.geneco.io 8.8.8.8

Checklist nhanh
bash# 1. 

# 2. Record có tồn tại trong Route53 không?
nslookup production-genesis-iam-be.geneco.io <NS-của-Route53-zone>
# Ví dụ: nslookup production-genesis-iam-be.geneco.io ns-123.awsdns-45.com

# 3. Bypass local DNS
nslookup production-genesis-iam-be.geneco.io 8.8.8.8
→ Nếu NS từ `8.8.8.8` **khác** với NS trong Route53 Hosted Zone → domain registrar chưa update nameserver.


---
Các lệnh làm việc với Domain / DNS

nslookup example.com                    # query A record
nslookup -type=NS example.com          # nameserver
nslookup -type=MX example.com          # mail record
nslookup -type=TXT example.com         # TXT/SPF/DKIM
nslookup -type=CNAME sub.example.com   # CNAME
nslookup -type=SOA example.com         # SOA (TTL, admin)
nslookup example.com 8.8.8.8           # query qua DNS cụ thể
